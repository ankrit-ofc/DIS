# Add these to your local .env to skip outbound SMS / email in dev.
# Without these (or with =true), the API will attempt real Sparrow / Resend calls
# and just log failures instead of crashing.

# External services — set to "false" to skip in local dev
SMS_ENABLED=true
EMAIL_ENABLED=true
